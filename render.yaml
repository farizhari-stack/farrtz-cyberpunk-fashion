services:
  - type: web
    name: farrtz-backend
    runtime: node
    region: singapore
    plan: free
    branch: master
    rootDir: server_backup
    buildCommand: npm install && npx prisma generate && npx prisma db push --accept-data-loss
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        value: postgresql://postgres:farizhari90@db.zsvvguibewyqkfklfubt.supabase.co:5432/postgres
      - key: JWT_SECRET
        value: farrtz_secure_secret
      - key: FRONTEND_URL
        value: https://farizhari-stack.github.io
      - key: PORT
        value: 5000
      - key: NODE_ENV
        value: production
